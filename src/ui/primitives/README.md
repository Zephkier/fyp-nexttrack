"primitives" are things like `<button>`, `<input>`, etc.

-   Creates consistency throughout website.

-   For example, it can ensure that every button looks the same.

For now, there are no primitives needed... yet.
